import type { Db } from '../db/connection.js';
export interface OperationTarget { operationId: number; repoName: string; servicePath: string; operationPath: string; operationName: string; sourceFile: string; sourceLine: number; }
export function findOperation(db: Db, servicePath: string | undefined, operationPath: string | undefined): OperationTarget | undefined { if (!operationPath) return undefined; const row = db.prepare(`SELECT o.id operationId,r.name repoName,s.service_path servicePath,o.operation_path operationPath,o.operation_name operationName,o.source_file sourceFile,o.source_line sourceLine FROM cds_operations o JOIN cds_services s ON s.id=o.service_id JOIN repositories r ON r.id=s.repo_id WHERE (? IS NULL OR s.service_path=?) AND (o.operation_path=? OR o.operation_name=?) ORDER BY CASE WHEN s.service_path=? THEN 0 ELSE 1 END LIMIT 1`).get(servicePath, servicePath, operationPath, operationPath.replace(/^\//, ''), servicePath) as OperationTarget | undefined; return row; }
