name: cf-request-runner

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish @saptools/cf-request-runner to npm
        required: false
        type: boolean
        default: false
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cf-request-runner.yml"
      - "packages/cf-request-runner/package.json"
      - "packages/cf-request-runner/README.md"
      - "packages/cf-request-runner/src/**"
      - "packages/cf-request-runner/tests/**"
      - "packages/cf-request-runner/playwright.config.ts"
      - "packages/cf-request-runner/tsconfig.json"
      - "packages/cf-request-runner/tsup.config.ts"
      - "packages/cf-request-runner/vitest.config.ts"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/cf-request-runner.yml"
      - "packages/cf-request-runner/package.json"
      - "packages/cf-request-runner/README.md"
      - "packages/cf-request-runner/src/**"
      - "packages/cf-request-runner/tests/**"
      - "packages/cf-request-runner/playwright.config.ts"
      - "packages/cf-request-runner/tsconfig.json"
      - "packages/cf-request-runner/tsup.config.ts"
      - "packages/cf-request-runner/vitest.config.ts"

permissions:
  contents: read

concurrency:
  group: cf-request-runner-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Build and verify cf-request-runner
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter @saptools/cf-request-runner lint

      - name: Typecheck
        run: pnpm --filter @saptools/cf-request-runner typecheck

      - name: Build
        run: pnpm --filter @saptools/cf-request-runner build

      - name: Run unit tests
        run: pnpm --filter @saptools/cf-request-runner test:unit

      - name: Run e2e tests
        run: pnpm --filter @saptools/cf-request-runner test:e2e

      # This E2E suite uses a local fake HTTP service and does not require live SAP CF credentials.
      - name: Verify publish tarball
        run: npm pack --dry-run
        working-directory: packages/cf-request-runner

  publish:
    name: Publish cf-request-runner to npm
    runs-on: ubuntu-latest
    needs: ci
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && inputs.publish == true && github.ref == 'refs/heads/main')
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm --filter @saptools/cf-request-runner build

      - name: Check whether version is already published
        id: publish_check
        working-directory: packages/cf-request-runner
        run: |
          package_name="$(node -p "require('./package.json').name")"
          package_version="$(node -p "require('./package.json').version")"

          if npm view "${package_name}@${package_version}" version >/dev/null 2>&1; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "Version ${package_version} is already published. Skipping npm publish."
          else
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish
        if: steps.publish_check.outputs.should_publish == 'true'
        run: npm publish --provenance --access public
        working-directory: packages/cf-request-runner
