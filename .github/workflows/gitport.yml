name: gitport

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish @saptools/gitport to npm
        required: false
        type: boolean
        default: false
  push:
    branches:
      - main
    paths:
      - ".github/workflows/gitport.yml"
      - "packages/gitport/package.json"
      - "packages/gitport/README.md"
      - "packages/gitport/src/**"
      - "packages/gitport/tests/**"
      - "packages/gitport/playwright.config.ts"
      - "packages/gitport/tsconfig.json"
      - "packages/gitport/tsup.config.ts"
      - "packages/gitport/vitest.config.ts"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/gitport.yml"
      - "packages/gitport/package.json"
      - "packages/gitport/README.md"
      - "packages/gitport/src/**"
      - "packages/gitport/tests/**"
      - "packages/gitport/playwright.config.ts"
      - "packages/gitport/tsconfig.json"
      - "packages/gitport/tsup.config.ts"
      - "packages/gitport/vitest.config.ts"

permissions:
  contents: read

concurrency:
  group: gitport-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Build and verify gitport
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter @saptools/gitport lint

      - name: Typecheck
        run: pnpm --filter @saptools/gitport typecheck

      - name: Build
        run: pnpm --filter @saptools/gitport build

      - name: Run unit tests
        run: pnpm --filter @saptools/gitport test:unit

      - name: Run e2e tests
        run: pnpm --filter @saptools/gitport test:e2e

      - name: Verify publish tarball
        run: npm pack --dry-run
        working-directory: packages/gitport

  publish:
    name: Publish gitport to npm
    runs-on: ubuntu-latest
    needs: ci
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && inputs.publish == true && github.ref == 'refs/heads/main')
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm --filter @saptools/gitport build

      - name: Check whether version is already published
        id: publish_check
        working-directory: packages/gitport
        run: |
          package_name="$(node -p "require('./package.json').name")"
          package_version="$(node -p "require('./package.json').version")"

          if npm view "${package_name}@${package_version}" version >/dev/null 2>&1; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "Version ${package_version} is already published. Skipping npm publish."
          else
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish
        if: steps.publish_check.outputs.should_publish == 'true'
        run: npm publish --provenance --access public
        working-directory: packages/gitport
