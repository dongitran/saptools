name: cf-inspector

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish @saptools/cf-inspector to npm
        required: false
        type: boolean
        default: false
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cf-inspector.yml"
      - "packages/cf-inspector/package.json"
      - "packages/cf-inspector/README.md"
      - "packages/cf-inspector/src/**"
      - "packages/cf-inspector/tests/**"
      - "packages/cf-inspector/playwright.config.ts"
      - "packages/cf-inspector/tsconfig.json"
      - "packages/cf-inspector/tsup.config.ts"
      - "packages/cf-inspector/vitest.config.ts"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/cf-inspector.yml"
      - "packages/cf-inspector/package.json"
      - "packages/cf-inspector/README.md"
      - "packages/cf-inspector/src/**"
      - "packages/cf-inspector/tests/**"
      - "packages/cf-inspector/playwright.config.ts"
      - "packages/cf-inspector/tsconfig.json"
      - "packages/cf-inspector/tsup.config.ts"
      - "packages/cf-inspector/vitest.config.ts"

permissions:
  contents: read

concurrency:
  group: cf-inspector-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Build and verify cf-inspector
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace deps and cf-inspector
        run: pnpm --filter @saptools/cf-inspector... build

      - name: Lint
        run: pnpm --filter @saptools/cf-inspector lint

      - name: Typecheck
        run: pnpm --filter @saptools/cf-inspector typecheck

      - name: Run unit tests
        run: pnpm --filter @saptools/cf-inspector test:unit

      - name: Run e2e tests
        run: pnpm --filter @saptools/cf-inspector test:e2e

      - name: Verify publish tarball
        run: pnpm pack
        working-directory: packages/cf-inspector

  publish:
    name: Publish cf-inspector to npm
    runs-on: ubuntu-latest
    needs: ci
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && inputs.publish == true && github.ref == 'refs/heads/main')
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace deps and cf-inspector
        run: pnpm --filter @saptools/cf-inspector... build

      - name: Check whether version is already published
        id: publish_check
        working-directory: packages/cf-inspector
        run: |
          package_name="$(node -p "require('./package.json').name")"
          package_version="$(node -p "require('./package.json').version")"

          if npm view "${package_name}@${package_version}" version >/dev/null 2>&1; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "Version ${package_version} is already published. Skipping npm publish."
          else
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish
        if: steps.publish_check.outputs.should_publish == 'true'
        run: pnpm publish --provenance --access public --no-git-checks
        working-directory: packages/cf-inspector
